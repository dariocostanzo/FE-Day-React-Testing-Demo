// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';

// Suppress console.log output during tests (optional - comment out for debugging demos)
// global.console.log = jest.fn();

// Configure testing-library to wait longer for async operations (useful for slow CI)
// import { configure } from '@testing-library/react';
// configure({ asyncUtilTimeout: 5000 });

// Global beforeEach/afterEach hooks can be added here
// Example: Reset all mocks before each test
// beforeEach(() => {
//     jest.clearAllMocks();
// });
